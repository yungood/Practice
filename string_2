#include<iostream>
#include<string>
using namespace std;

// 프로그래머스 문제 : Level 1(문자열 다루기 기본)

/*
	문제) 길이가 n이고, "수박수박수박수..."와 같은 패턴을 유지하는 문자열을 리턴하는 함수, solution을 작성하시오.

	예시) n이 4이면 "수박수박"을 리턴하고 3이면 "수박수"를 리턴한다.

	해결방안)

	(1)  수, 박, 수, 박을 번갈아가면서 찍어주기(홀수 : 수, 짝수 : 박)
	(2)  '수박' 을 하나로 묶어서 짝수일때는 '수박'의 반복, 홀수면 '수박'에 '수'가 추가
	(3) 삼항연산자 사용

*/

int main(){
	
	int n;
	string s = "";
	
	cin >> n;

	/*
	(1) 첫번째 방법

	for (int i = 1; i <= n; i++) {
		if (i%2 != 0) { // 홀수이면
			cout << "수";
		}
		else { // 짝수이면
			cout << "박";
		}	
	}
	*/


	/* 
	(2) 두번째 방법 

	for (int i = 0; i < (n/2); i++) { // 짝수까지는
		s += "수박";
	}

	if (n % 2 != 0) { // 홀수이면
		s += "수";
	}

	cout << s << "\n";

	*/


	/*
	(3) 삼항연산자 사용

	for (int i = 1; i <= n; i++) {
		s += (i % 2 != 0) ? "수" : "박"; // 홀수이면 '수', 짝수이면 '박'
	}

	cout << s << "\n";
	
	*/

}
